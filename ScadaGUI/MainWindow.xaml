<Window x:Class="ScadaGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SCADA Application" Height="450" Width="850" 
        Closing="Window_Closing" Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="dataGridTags" Grid.Row="0" AutoGenerateColumns="True" 
                  IsReadOnly="True" SelectionMode="Single" Margin="0,0,0,10">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding AlarmStatus}" Value="Active">
                            <Setter Property="Background" Value="#FFFFC0CB"/>
                            <Setter Property="Foreground" Value="Black"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding AlarmStatus}" Value="Acknowledged">
                            <Setter Property="Background" Value="#FFFFE4B5"/>
                            <Setter Property="Foreground" Value="Black"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Content="Dodaj tag" Click="AddTag_Click" Height="30" Width="130" Margin="0,0,10,0"/>
            <Button Content="Ukloni izabrani tag" Click="RemoveTag_Click" Height="30" Width="130" Margin="0,0,10,0"/>
            <Button Content="Dodaj alarm" Click="AddAlarm_Click" Height="30" Width="130" Margin="0,0,10,0"/>
            <Button Content="Potvrdi alarm (ACK)" Click="AcknowledgeAlarm_Click" Height="30" Width="140"/>
        </StackPanel>
    </Grid>
</Window>